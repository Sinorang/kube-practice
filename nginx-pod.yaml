# 매니페스트 파일
# : 쿠버네티스에서 다양한 리소스 파드나 그 이외의 리소스들을 생성하고 관리하기 위해 사용하는 파일
apiVersion: v1
kind: Pod

metadata:
  name: nginx-pod

spec:
  containers:
    - name: nginx-container
      image: nginx
      ports:
        - containerPort: 80 # 단지 문서화의 역할일 뿐, 다른 값을 적는다 해서 그것이 해당 컨테이너에 적용되지는 않음